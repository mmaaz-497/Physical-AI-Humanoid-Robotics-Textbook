# Qdrant Vector Database Configuration
QDRANT_URL=https://your-cluster.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key_here
QDRANT_COLLECTION=physical_ai_textbook

# Gemini Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# Database Configuration (Required for Authentication)
NEON_DATABASE_URL=postgresql://user:password@host/database

# Authentication Configuration (Required for Better Auth JWT validation)
BETTER_AUTH_JWT_SECRET=your-256-bit-secret-key-change-this-in-production

# API Configuration
# For production, set this to your frontend URL
# Example: https://your-site.vercel.app
CORS_ORIGINS=http://localhost:3000

# Local Development Only
API_HOST=0.0.0.0
API_PORT=8000

# Retrieval Configuration (Optional - defaults are fine)
SIMILARITY_THRESHOLD=0.6
MAX_TOP_K=10
DEFAULT_TOP_K=5

# Rate Limiting (Optional - defaults are fine)
MAX_QUERY_LENGTH=500
MAX_SELECTION_TEXT_LENGTH=5000
