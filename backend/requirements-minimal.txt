# FastAPI and Web Framework (REQUIRED)
fastapi
uvicorn[standard]
python-multipart

# Data Validation (REQUIRED)
pydantic>=2.0
pydantic-settings

# Environment Variables (REQUIRED)
python-dotenv

# Vector Database (REQUIRED for retrieval)
qdrant-client

# HTTP Client (REQUIRED)
httpx

# Database (lightweight - for ORM models even if DB not used)
sqlalchemy
